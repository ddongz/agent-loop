# SentinelLoop 规约形成过程

## 1. 方法与工具

项目使用 Codex App 与 Superpowers 6.2.0。2026-08-13 环境审计发现 Superpowers 尚未安装，随后经用户确认安装 `superpowers@openai-curated-remote`，并读取 `using-superpowers`、`brainstorming` 及 Codex 适配说明。

项目遵循 brainstorming 的设计门：在设计批准前没有创建任何实现源码或脚手架。用户在 2026-08-14 明确要求后续非关键方向全部采用推荐项，并尽量连续推进；因此后续剩余设计采用已批准方向下的推荐方案，不再逐项停顿。

## 2. Brainstorming 关键节点

### 2.1 主贡献选择

问题：Harness 六维中重点深入确定性反馈、安全治理还是记忆/上下文？  
用户选择：确定性反馈闭环。  
影响：项目的核心价值从“通用 Coding Agent”收敛为可分类失败、衡量进展、检测震荡和确定停机的 TDD Harness。

### 2.2 任务和生态范围

问题：修复测试、按需求新增功能或两者；目标仓库选择 TypeScript/Node、多语言或 Python？  
用户选择：按需求新增功能；TypeScript/Node.js。  
影响：形成“自然语言需求 → Agent 生成测试 → 红灯 → 实现”的状态机，并能针对 Vitest/Jest/tsc/ESLint 做更深的反馈解析。

### 2.3 产品形态与分发

问题：CLI + WebUI、纯 WebUI或 CLI；npm、二进制或 Docker？  
用户说明：只开发 CLI，助教已允许不做 WebUI，只提供 GitHub 等 Release 链接；选择 npm + GitHub Release。  
影响：删除 WebUI 范围，把工程投入集中到反馈闭环；记录为相对原始通用要求的有依据偏离。

### 2.4 供应商、权限和凭据

用户选择：OpenAI 兼容接口；工作区内自动修改、危险动作审批；系统凭据管理器。  
影响：保留通用 LLMClient 和 Scripted Mock；使用结构化工具与策略层；Key 不进入配置、日志或任务状态。

### 2.5 输入、TDD 和测试保护

用户选择：自然语言直接输入；Agent 先生成测试；红灯后冻结新增测试。  
影响：设计双层红灯有效性判断、冻结前用户一次确认、SHA-256 基线和实现阶段写前拦截。

### 2.6 停机、工具和仓库安全

用户选择：组合停机；结构化文件工具；脏工作区拒绝启动。  
影响：不暴露任意 shell；同类失败 3 次无进展、总 8 轮或预算耗尽暂停；保护用户未提交工作。

### 2.7 批量推荐项

用户一次确认全部推荐项：

- npm/pnpm/yarn 自动识别；
- 自动发现验证命令并允许配置覆盖；
- 终端一次性审批；
- `.sentinelloop/` 持久化并默认忽略；
- Chat Completions 风格 tool calling；
- Windows/macOS/Linux、Node.js 22+、三平台 CI。

## 3. 三轮关键迭代节选与处理决策

### 迭代一：从宽泛 Harness 到确定性反馈产品

> 助手：希望重点深入确定性反馈闭环、安全治理，还是记忆与上下文？  
> 用户：第一项。

处理：采纳反馈闭环作为 main contribution，同时保留治理、记忆等维度的最低可运行实现，以满足 A 类完整性要求。

### 迭代二：从普通代码生成到可信 TDD

> 助手：新增功能所需测试由 Agent 先生成、只用已有测试，还是用户预先提供？  
> 用户：第一个。  
> 助手：如何防止 Agent 降低断言或删除测试？  
> 用户：冻结新增测试。

处理：把测试生成和实现分成不同状态；红灯确认后建立哈希基线，治理层在执行前保护测试，而非依赖 prompt。

### 迭代三：从原始 WebUI 要求到课程最新口径

> 用户：只开发 CLI；助教已经说明可以不必开发 WebUI，只提供一个 release 链接即可，如 GitHub。  
> 助手：确认 CLI-only，并建议 npm 包 + GitHub Release。  
> 用户：第一个。

处理：接受课程人员的后续说明优先于原始文档；将偏离原因写入 SPEC、AGENT_LOG 和交付文档，不假装原始要求不存在。

## 4. 方案比较

### 方案一：阶段化状态机（采用）

将预检、测试生成、红灯确认、冻结、实现和验证划为明确状态。优点是 TDD、测试保护、审批和恢复都能被确定性测试；代价是探索自由度较低。

### 方案二：自由 ReAct 循环（拒绝）

更灵活且代码较少，但难以证明先测试后实现，容易依赖提示词纪律，不利于本作业“机制必须是代码”的要求。

### 方案三：Planner–Executor 双层循环（暂缓）

复杂任务适应性更强，但会分散反馈闭环的主要贡献，并显著增加上下文、预算和 mock 测试复杂度。

## 5. AI 建议的采纳、修改和拒绝

### 采纳

- 反馈闭环作为重点；
- TypeScript/Node.js 单生态；
- CLI + npm Release；
- 结构化工具；
- 测试冻结；
- 系统凭据管理器；
- 组合停机和三平台 CI。

### 用户修正

- 助手最初推荐 CLI + WebUI；用户依据助教新说明删去 WebUI。该修正减少范围并对齐最新验收口径。
- 助手推荐结构化任务文件；用户选择自然语言直接输入。为降低歧义风险，设计增加冻结前测试 diff 与需求映射确认。

### 拒绝或延期

- 多语言、多供应商原生适配、多 Agent 编排、通用 shell、WebUI 和自动 Git 远程写入均延期，避免稀释核心机制。

## 6. Brainstorming 反思（阶段性）

做得好的地方：连续的范围选择迫使项目从宽泛“做一个 Coding Agent”收敛到可测试的状态机；测试冻结问题直接暴露了普通 Agent 可能伪造成功的治理缺口；方案比较使主要贡献与课程评分判据一致。

不足：逐题确认在决策密集阶段造成较多停顿，用户随后明确要求只在关键处询问。后续采用“关键门询问、其余推荐项批量确认”的方式更高效。另一个局限是自然语言需求与测试是否充分匹配无法完全由确定性代码证明，仍需冻结前的人类确认。

## 7. 陌生智能体冷启动（待执行）

按课程要求，计划完成后将使用与主开发智能体不同类型、无对话历史的新 agent，仅提供 `SPEC.md` 和 `PLAN.md`，执行 1–2 个前置实现 task，并要求不确定时停止提问。将记录：

- 暂停点和问题；
- 对规格的错误或不同解读；
- 与预期的产出偏差；
- 对 SPEC/PLAN 的修订；
- 修订前后关键 diff。

在冷启动完成前不进入正式实现。
