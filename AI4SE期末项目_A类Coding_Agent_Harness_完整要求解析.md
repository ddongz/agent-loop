# AI4SE 期末项目 A 类 Coding Agent Harness：完整作业要求解析

> 解析依据：`AI4SE_Final_Project_通用要求.md` + `AI4SE_Final_Project_A_Coding_Agent_Harness.md`  
> 适用对象：选择“自选任务一：Coding Agent Harness”的学生。  
> 阅读原则：A 类要求不是独立题目，完整要求始终是“通用要求 + A 类专属要求”的叠加。

## 1. 作业本质

这不是一项“调用 LLM 做出一个能运行的 Demo”即可完成的普通开发作业，而是一项同时考查产品设计、智能体协作流程、软件工程质量和个人判断力的综合项目。

你需要使用 Superpowers 的完整方法论，自行实现一个面向软件开发场景的 Coding Agent Harness。最终被评价的不是单一代码成品，而是以下四部分的组合：

1. **软件产品**：一个真实可用、有工程深度、可分发和可部署的 Coding Agent Harness；
2. **自研内核**：由你自己的代码实现 agent 循环、工具、治理、反馈、记忆、配置和停机等机制；
3. **工程过程**：有证据证明你执行了规约、计划、冷启动、worktree、subagent、TDD 和两阶段评审；
4. **个人判断**：能够说明你如何质疑、修正和验收 AI 的输出，并对 Superpowers 方法论形成自己的批判性认识。

核心判断题是：当真实 LLM 被移除、网络被切断、模型输出由 mock 控制时，你编写的 harness 是否仍能被确定性测试证明是一个可靠的执行系统？

## 2. 什么是本题要求的 Coding Agent Harness

本题采用以下理解：

`Coding Agent = LLM 的下一步决策能力 + Harness 的可靠执行能力`

LLM 只负责提出下一步动作；其余工程由 harness 负责：

- 组织上下文并调用模型；
- 将模型输出解析成结构化动作；
- 分发文件、shell、构建、测试等工具；
- 保存并按需检索跨轮次、跨会话记忆；
- 在危险动作前拦截，必要时进入人工审批（HITL）；
- 采集测试、lint、类型检查等客观信号并回灌；
- 用声明式配置约束 agent 的行动范围；
- 判断继续、暂停、失败或完成。

因此，一个聊天 API 包装器、一个提示词集合，或在现成 agent 框架上挂几个工具，都不等于本题要求的 harness。

## 3. 硬性要求、条件要求与鼓励项

### 3.1 硬性要求

- 项目必须由个人独立完成，不允许组队；
- 必须使用 Superpowers，并如实遵循规定的七步工作流；
- 在 `SPEC.md`、`PLAN.md` 完成并通过陌生智能体冷启动验证前，禁止正式编写实现代码；
- 必须严格执行 TDD：先红、再绿、再重构，不接受先实现后补测试；
- 必须自行实现 harness 内核，不得调用现成 agent runner 代替；
- 决策、工具、记忆、治理、反馈、配置六个维度都要有可运行的最低实现；
- 必须选择其中一个机制密集维度深入实现，形成主要贡献；
- 所有核心机制必须能在 mock/stub LLM 下做确定性离线测试；
- 必须提交三个指定行为的机制演示；
- API Key 不得硬编码、提交到 Git 或泄露到日志/终端历史/明文配置；
- 至少实现一种安全凭据存储及录入、状态查看、更新、清除流程；
- 至少选择容器、二进制或包管理器中的一种进行实际分发；
- 项目至少包含 3 个职责清晰的功能模块，并提供一键测试；
- 必须使用 worktree、subagent、两阶段评审以及规范的 commit/PR 工作流；
- 必须完成全部规定文档、CI、分发、部署和过程证据；
- 最后一次 CI/CD 必须为 pass；
- 按最终交付清单，必须提供可访问的 WebUI URL。

### 3.2 条件性要求

- 包含前端/UI 时，强烈推荐使用 Open Design，并在 `SPEC.md` 中说明设计系统和 skill；纯 CLI/后端部分可豁免 Open Design。
- 采用容器分发时，CI 还须构建镜像。
- 采用二进制分发时，要说明目标平台、CPU 架构、签名和首次运行系统拦截；鼓励 CI 产出可下载构件。
- 若做服务端，正文把云部署描述为可选；但最终清单要求 WebUI URL，稳妥处理方式见第 14 节。

### 3.3 鼓励项

- 组合使用多种智能体并比较表现；
- 在保证深度的前提下提供多种分发形式；
- 对 Superpowers 的适用条件、假设和局限提出有实际证据的批判。

## 4. 自研边界：什么可以使用，什么必须自己写

### 4.1 开发过程中可以使用

- Codex、Claude Code、Cursor、Gemini CLI 等编码智能体；
- Superpowers 的 subagent、skills、hooks、memory 和内置工具；
- LLM 供应商的单次对话/补全 API；
- HTTP 库、结构化解析库、数据库、向量库等底层零件。

这些能力只能帮助你开发，不能被当作交付产品自身的 harness 实现。

### 4.2 交付产品必须自己实现

- agent 主循环：组织上下文 → 调用 LLM → 解析动作 → 分发执行 → 回灌结果 → 停机判断；
- 可注入 mock 的 LLM 抽象层；
- 动作 schema、解析与参数校验；
- 工具注册、权限检查与分发；
- 危险动作识别、拦截与人工审批流程；
- 客观反馈采集、解析、分类和回灌；
- 记忆写入、组织、检索和上下文选择；
- 配置读取、校验和执行；
- 完成、失败、预算耗尽、无进展等停机逻辑。

### 4.3 明确不允许

不得使用下列高层框架或宿主能力替代自己的 harness 内核：

- LangChain `AgentExecutor`；
- AutoGen；
- CrewAI；
- LlamaIndex agent；
- 编码智能体 SDK 自带的 agent runner；
- 宿主编码智能体的 agent loop、skills、治理 hooks 或 memory。

允许使用零件，但“循环 + 工具 + 治理 + 反馈 + 记忆 + 停机”的组装和控制必须体现在你自己的代码中。

## 5. 六个维度：基础必须完整，一维必须深入

| 维度 | 最低可运行实现 | 关键确定性测试 |
|---|---|---|
| 决策 | 上下文构造、LLM 抽象、动作解析、循环与停机 | mock 返回动作序列，循环按预期推进并停止 |
| 工具 | 工具注册/分发，支持必要的 coding 操作 | 已知工具正确执行；未知工具或非法参数被拒绝 |
| 记忆 | 保存项目约定、历史决策或代码库知识并按需检索 | 写入、读取、筛选和跨会话恢复稳定可验 |
| 治理 | 风险分类、动作拦截、范围围栏或 HITL | 构造危险动作后必然拦截；批准/拒绝路径确定 |
| 反馈 | 测试/lint/类型检查等校验器及反馈回灌 | 注入失败后，下一轮上下文含标准化反馈 |
| 配置 | 允许工具、路径、预算、策略等声明式约束 | 合法配置生效；非法配置失败；默认值明确 |

配置文件、规则文件、skill 和提示词只属于“内容物”，不计入 harness 的实现深度。真正计入的是读取、验证并执行这些规则的代码机制。

### 5.1 可选的重点维度

必须选一个机制密集维度作为 main contribution，并在 SPEC、测试和机制演示中保持一致：

- **治理**：风险分级、路径/命令/网络范围围栏、HITL 状态机、审批超时、拒绝、恢复执行、审计日志；
- **反馈闭环**：多验证器、失败分类、反馈去噪、重试预算、无进展检测、多轮自我修正；
- **工具/扩展**：类型安全工具协议、插件注册、权限作用域、并发、超时、取消、隔离或多 agent 编排；
- **记忆/上下文**：自研存储与检索策略、相关性、版本化、上下文预算、过期/冲突处理、敏感信息过滤。

如果选择记忆作为重点，可以使用数据库或向量库作为底层零件，但记忆的组织、检索、选择和注入策略必须由自己的 harness 代码实现。

## 6. SPEC 必须设计的四类 coding 机制

### 6.1 动作/工具

要明确 agent 能执行哪些操作，如读取/修改/搜索文件、执行受限 shell、运行构建、测试、lint、类型检查和获取版本控制状态。每种工具不能只列名称，还要写清：

- 输入与输出；
- 参数 schema；
- 文件路径和工作区边界；
- 超时与取消；
- 输出截断；
- 错误类型；
- 权限和安全限制。

### 6.2 客观反馈信号

反馈必须由代码实现的校验器或传感器产生，例如：

- 单元测试退出码和失败用例；
- lint 违规；
- 类型检查错误；
- 构建成功或失败；
- 补丁是否越界；
- 验收测试是否通过。

完整链路应是：执行产物 → 解析确定性结果 → 分类成功/失败 → 生成标准化反馈 → 回灌下一轮循环。只在 prompt 中写“请检查并修复”不算实现反馈机制。

### 6.3 危险动作

要定义危险等级、识别依据、边界和审批行为，例如：

- 递归删除或修改工作区外文件；
- 破坏数据库的命令；
- 读取敏感文件或修改凭据；
- 外部网络写操作；
- 发布包、部署、push 或创建外部资源；
- 高权限 shell 命令。

护栏必须接收结构化动作并确定性返回“允许、拒绝或需要审批”，不能依赖 LLM 自律。

### 6.4 记忆

要说明跨会话保存什么、如何存储和检索、何时注入模型上下文，以及为什么不全量载入历史。典型内容包括项目约定、架构决策、已完成任务、失败尝试、代码库摘要和用户审批结论。

同时应设计：

- 存储格式；
- 索引/检索方式；
- 更新、冲突和淘汰策略；
- 上下文预算；
- 敏感信息边界。

## 7. “机制必须是代码”的硬判据

### 7.1 算作实现

- `guardrail(action)` 按确定性规则识别并拦截危险动作；
- `tool_dispatch(action)` 校验 schema 后调用相应工具；
- `validator(result)` 解析测试输出并生成结构化反馈；
- `memory.query(context)` 按规则检索相关记忆；
- `termination(state)` 根据完成信号、预算、轮次或错误停止；
- 审批状态机持久化待审批动作并在批准后恢复。

### 7.2 不算实现

- 系统提示词写“不要执行危险命令”；
- 提示词要求“测试失败后自行修复”；
- 配置文件罗列工具，但没有自己的分发与校验代码；
- 调用现成 agent runner 处理循环、工具、重试和停机；
- 只用真实 LLM 的偶然正确输出证明机制有效。

### 7.3 自查标准

把真实 LLM 换成返回受控动作的 mock 后，工具分发、危险拦截、反馈回灌、记忆读写和停机仍应能够独立、稳定地通过单元测试。做不到的部分很可能只是提示词能力，而非真正的 harness 工程。

## 8. 强制 Superpowers 工作流

完整顺序应为：

`brainstorming` → `writing-plans` → 陌生智能体冷启动 → 修订 SPEC/PLAN → `using-git-worktrees` → `subagent-driven-development` / `executing-plans` → `test-driven-development` → `requesting-code-review` → `finishing-a-development-branch`

### 8.1 Brainstorming 与 SPEC

- 让 `brainstorming` 追问目标用户、范围、失败场景和工程约束；
- 学生逐块确认、质疑或修改设计，不能自动接受 AI 建议；
- 保存至少 3 轮关键迭代证据；
- 将确认后的设计沉淀为 `SPEC.md`。

### 8.2 Writing plans

- 用 `writing-plans` 把设计拆成细粒度 task；
- 原文建议每一步约 2–5 分钟；
- 每个 task 应能由一个新鲜 subagent 在一次会话中完成；
- 每个 task 必须写目标、文件路径、实现要点、失败测试和完整验证步骤；
- 明确依赖与可并行部分，为 worktree 做准备。

可以用层级化结构兼容两种颗粒度：一个可独立委派的 task 内含多个 2–5 分钟操作步骤。

### 8.3 陌生智能体冷启动

正式实现前必须：

- 使用与主开发智能体不同类型的第二个 agent；
- 开启全新 session，不共享历史或 memory；
- 只提供 `SPEC.md` 和 `PLAN.md`，不口头补充；
- 让其实现计划中的 1–2 个 task，约 1–2 小时；
- 明确要求遇到不确定即暂停询问，不允许自行猜测；
- 记录它的提问、误解、产出偏差；
- 根据结果修订 SPEC/PLAN，并保留修订前后关键 diff。

原文同时禁止冷启动验证前写正式实现。稳妥做法是把第二个 agent 的试作放入可丢弃的隔离分支/worktree，只保留它暴露的规约问题与修订证据；正式实现仍按 TDD 从失败测试开始。

### 8.4 实现阶段

- 每个独立功能/大模块使用一个 worktree，并对应一个 PR；
- 每个 task 派一个新的 subagent，只处理单一任务；
- 严格执行红—绿—重构；
- 每个 task 先做 spec 合规评审，再做代码质量评审；
- Critical issue 修复前不得进入下一 task；
- 每完成一个 task，更新 `PLAN.md` 并附 commit hash；
- 最后用 `finishing-a-development-branch` 决定合并、发 PR、保留或丢弃。

## 9. 三份前置文档

### 9.1 `SPEC.md`

必须包含：

1. 问题陈述：为谁解决什么问题，为什么值得做；
2. 至少 5 个符合 INVEST 的用户故事；
3. 按模块编写的功能规约：输入、行为、输出、边界、错误处理；
4. 非功能需求：性能、安全、可用性、可观测性；
5. 系统架构：组件图、数据流和外部依赖；
6. 数据模型：实体、字段、关系和约束；
7. 凭据与分发设计；
8. 技术选型与理由；
9. 各功能的客观验收标准；
10. 风险与未决问题；
11. A 类专属的“领域与机制设计”。

“领域与机制设计”建议包含：

```markdown
## 领域与机制设计
### Coding 任务边界与典型工作流
### 动作/工具及其输入输出、安全边界
### 客观反馈信号与回灌协议
### 危险动作分类、护栏与 HITL 状态机
### 跨会话记忆、检索与上下文预算
### 六维最低实现映射
### 重点维度、选择理由与主要贡献
### 各机制的代码模块与接口设计
### 移除真实 LLM 后的可测试性矩阵
```

### 9.2 `PLAN.md`

每个 task 建议统一写为：

```markdown
## Task N：任务名称
- 状态：待开始 / 进行中 / 已完成
- 目标：
- 依赖：
- 可并行于：
- 涉及文件：
- 实现要点：
- TDD 红灯：要先添加的失败测试及预期失败原因
- TDD 绿灯：使测试通过的最小实现
- 重构点：
- 验证命令与预期结果：
- 执行 subagent：
- 完成 commit：
```

不能只写“完成主循环”“增加测试”等无法独立委派和验收的大任务。

### 9.3 `SPEC_PROCESS.md`

它是规约形成过程的证据，而非 `SPEC.md` 摘要。必须包含：

- brainstorming 中有价值的问题及其影响；
- 至少 3 轮关键迭代对话节选；
- AI 建议的采纳、拒绝或修改及理由；
- 对 brainstorming 优缺点的反思；
- 陌生 agent 冷启动的操作方式；
- 它的提问、误解和产出偏差；
- SPEC/PLAN 修订前后的关键 diff。

## 10. TDD、测试和三个机制演示

### 10.1 TDD 纪律

每个 task 必须按以下顺序推进：

1. 编写失败测试；
2. 实际运行并看到预期失败；
3. 编写最少实现使其通过；
4. 在测试持续通过的前提下重构。

建议用独立 commit 和 `AGENT_LOG.md` 记录红灯输出、绿灯 commit 和重构结果。只有最终测试文件不足以证明执行了 TDD。

### 10.2 核心离线测试范围

| 测试对象 | 关键用例 |
|---|---|
| 主循环 | 多轮推进、完成、最大轮数、错误停机 |
| 动作解析 | 合法动作、未知动作、缺字段、畸形/恶意输入 |
| 工具分发 | 正确工具、参数校验、执行失败、超时 |
| 治理 | 安全动作允许、危险动作拦截、批准/拒绝 |
| 反馈 | 成功/失败解析、分类、回灌下一轮上下文 |
| 记忆 | 写入、读取、筛选、恢复、敏感信息边界 |
| 配置 | 有效配置、非法配置、作用域和默认值 |
| 停机 | 完成、预算耗尽、无进展、不可恢复错误 |

这些测试必须由 mock/stub LLM 驱动，不依赖网络和真实 LLM，并可通过一个命令全部运行。

### 10.3 机制演示一：治理拦截危险动作

mock LLM 请求危险动作 → 护栏识别风险 → 动作未到达执行层 → 系统拒绝或进入待审批状态 → 输出/审计记录给出原因。

### 10.4 机制演示二：失败使下一步动作改变

mock LLM 第一次产生动作 → 校验器注入失败 → 标准化反馈回灌 → mock LLM 因收到该反馈而返回不同的修复动作 → 再次验证。

关键是证明第二步动作确实受反馈影响，而不是 mock 无视输入、机械播放固定序列。

### 10.5 机制演示三：重点维度的确定性行为

必须与 main contribution 对齐，例如：

- 治理重点：审批后从持久化状态恢复执行；
- 反馈重点：同类失败达到阈值后停止无效重试；
- 工具重点：未授权插件即使被模型请求也无法调用；
- 记忆重点：只检索与当前任务相关且未过期的决策。

演示可采用自动化测试、可重复脚本或 WebUI 演示模式，但必须提供明确命令、预期输出和成功判据。录屏只能作为补充，不能替代可执行演示。

## 11. API Key 与安全边界

A 类项目要同时解决两层安全问题。

### 11.1 产品凭据安全

明确禁止：

- 在源码、测试或示例中写真实 key；
- 将真实 key 提交到任何 Git 历史；
- 写入日志、终端 history 或明文配置；
- 查看凭据状态时回显明文。

至少要实现一种安全存储，如操作系统钥匙串、密钥管理服务或带主密码的加密文件，并提供：

- 首次运行隐藏录入；
- 查看是否已配置，但不显示明文；
- 更新；
- 清除；
- 安全读取；
- 日志和错误脱敏。

环境变量可以作为来源；若使用 `.env`，必须加入 `.gitignore` 并说明它仍是明文且进程环境可见。只提供 `.env` 是否足以满足“安全存储”存在解释风险，最稳妥方案是把 OS keyring 或加密存储作为默认实现，把 `.env` 仅作为明确标注风险的兼容方式。

`SPEC.md` 的威胁模型至少应分析源码/Git、日志、终端历史、本地明文文件、进程环境、UI 回显、异常栈和 CI secret 泄漏。

### 11.2 Agent 行动安全

还要限制 agent 的文件、shell、网络、发布和外部副作用，并在必要时进入 HITL。安全保存 key 不能替代行动治理，行动护栏也不能替代凭据安全，两层都必须完成。

## 12. 分发、WebUI 与新机器验收

### 12.1 分发是必做项

至少选择一种：

- **容器**：单条 `docker build` 和单条 `docker run` 可启动，并推送到公开 registry；
- **原生二进制**：单文件可执行，说明平台、CPU 架构、签名和系统拦截；
- **包管理器**：通过 npm、PyPI、cargo、Homebrew 等提供明确安装命令。

只提交源码及开发环境启动步骤不算完成分发。

### 12.2 WebUI

虽然云部署正文使用条件性表述，但最终交付清单明确要求提供可访问的 WebUI URL。最稳妥方案是即使核心是 CLI，也提供一个最小但真实可用的 WebUI，并让它能够触发或展示 harness 核心能力，而不是只有静态介绍页面。

### 12.3 新机器验收

建议在干净环境完整演练：获取产物 → 安装/启动 → 首次安全录入 key → 查看状态 → 执行核心场景 → 更新 key → 清除 key → 运行测试。记录平台、命令和结果，作为分发可信度证据。

## 13. Git、worktree、subagent 和过程证据

- 不能一次 commit 提交全部代码；
- 每个独立功能/大模块的 worktree 对应一个 PR；
- 每个 task 使用一个新鲜 subagent；
- commit message 或 PR 描述标注 subagent；
- 记录人工修改了什么、为什么；
- 每完成一个 task，更新 `PLAN.md` 状态并附 commit hash；
- 提交前扫描当前文件和 Git 历史中的真实凭据。

### 13.1 两阶段评审

每个 task 必须依次完成：

1. **Spec 合规检查**：实现是否满足需求、边界、错误处理和验收标准；
2. **代码质量检查**：正确性、安全性、可维护性、测试质量和潜在缺陷。

Critical issue 要有发现、修复和复验的完整记录。

### 13.2 `AGENT_LOG.md`

每条记录至少包含：

- 时间戳和 task 编号；
- 触发的 Superpowers skill；
- 关键 prompt/context；
- subagent 关键输出或 commit hash；
- 人工干预内容与理由；
- 学到的教训。

任何对 Superpowers 七步流程的偏离都必须在这里如实解释。日志应随开发实时维护，不能只在项目结束后补一份概括。

## 14. 原文的两处口径差异与稳妥方案

### 14.1 GitHub 与 NJU Git / GitLab CI

正文要求公开 GitHub 仓库、GitHub PR 工作流和 GitHub Actions；最终清单则要求通过 NJU Git 仓库链接提交，并明确 `.gitlab-ci.yml` 中必须有名为 `unit-test` 的 job。

在没有教师进一步澄清前，最稳妥方案是双满足：

1. 使用同一套 Git 历史同步 GitHub 与 NJU Git；
2. 在 GitHub 保留 PR/worktree 证据；
3. 以 NJU Git 作为正式提交入口；
4. 同时提供 `.gitlab-ci.yml` 和 GitHub Actions；
5. 两套 CI 调用同一个本地一键测试命令；
6. 保证最后一次 CI/CD 为 pass。

### 14.2 “云部署可选”与“WebUI URL 必须”

§4.11 表示带服务端项目“可选择”云部署，但最终清单第 9 项明确写明必须提供可访问的 WebUI。应按更具体、更靠后的最终交付清单执行：提供公网可访问的真实 WebUI。

如果课程后续发布更明确的书面说明，应以教师最新口径为准。

## 15. README 必须写清的内容

`README.md` 至少应包含：

- 项目简介和目标用户；
- 安装；
- 运行；
- 分发/构建命令；
- 目录结构；
- 安全边界；
- 项目/产物获取方式；
- key 在目标机器上的安全配置、查看状态、更新和清除；
- 已知限制：平台、架构和依赖前提；
- 机制演示命令和预期输出；
- WebUI URL；
- 部署架构与 CI/CD；
- 第三方代码及许可证。

## 16. 最终交付清单

| 编号 | 交付物 | 最低验收点 |
|---|---|---|
| 1 | `SPEC.md` | 10 类通用章节 + A 类“领域与机制设计” |
| 2 | `PLAN.md` | 任务细、含失败测试/验证、依赖/并行、完成 commit |
| 3 | `SPEC_PROCESS.md` | 至少 3 轮迭代、陌生 agent 冷启动、修订 diff |
| 4 | Harness 源码 | 自研主循环、工具、治理、反馈、记忆、配置和停机 |
| 5 | Mock LLM 测试 | 核心机制离线、确定性、无网络/真实 LLM 依赖 |
| 6 | 三个机制演示 | 危险拦截、失败改变下一步、重点维度行为 |
| 7 | Git/PR 历史 | 非单 commit；worktree/PR、subagent、人工修改可追踪 |
| 8 | 分发产物 | 容器/二进制/包至少一种，可按文档获取和运行 |
| 9 | `README.md` | 规定章节、key 管理、安全边界、限制、演示完整 |
| 10 | `AGENT_LOG.md` | 按时间记录 skill、prompt、输出、人工干预和教训 |
| 11 | CI 配置 | `.gitlab-ci.yml` 含 `unit-test`；稳妥起见兼有 GitHub Actions |
| 12 | CI/CD 记录 | 最后一次执行为 pass |
| 13 | `REFLECTION.md` | 1500–2500 字，由学生本人撰写 |
| 14 | WebUI URL | 截止前公网可访问，能展示或触发核心能力 |
| 15 | 凭据安全 | 无真实凭据；安全录入、存储、状态、更新、清除完整 |

## 17. `REFLECTION.md` 的要求

反思报告必须由学生本人撰写，禁止 AI 代写；AI 只能辅助润色且须标注。建议在开发过程中持续收集以下真实素材：

- 哪些 Superpowers skill 真正有价值，哪些形式大于实质；
- TDD 在具体任务中的成本和收益；
- subagent 能自主运行多久，何时开始偏离；
- 哪种 task 颗粒度效果最好；
- 一次 spec 不清导致 subagent 误解的具体案例；
- 最有效的 prompt/context 策略及依据；
- 凭据和分发要求促使你解决了哪些原本会忽略的问题；
- 如果重做会调整什么；
- Superpowers 假设了什么，这些假设在项目中是否成立。

## 18. 推荐模块划分

一种清晰但非强制的职责划分是：

```text
core/          主循环、状态、预算与停机
llm/           LLM 接口、mock、真实供应商适配器
actions/       动作 schema、解析与校验
tools/         工具注册、分发与执行适配器
governance/    风险分类、范围围栏、审批状态机
feedback/      测试/lint/构建传感器与反馈标准化
memory/        持久化、检索与上下文选择
config/        配置加载、schema 校验与策略执行
credentials/   key 的安全录入、读取、更新与清除
web/           对外 WebUI
tests/         单元、集成与机制演示
```

具体目录可不同，但 `SPEC.md`、源码模块和测试之间的职责映射必须一致。

## 19. 推荐执行顺序

1. 安装并验证 Superpowers；
2. 建立仓库和过程文档骨架，不写实现；
3. 通过 brainstorming 确认问题、用户和主要贡献维度；
4. 完成 `SPEC.md` 和 `SPEC_PROCESS.md` 初稿；
5. 使用 `writing-plans` 完成细粒度 `PLAN.md`；
6. 用不同类型的陌生 agent 冷启动 1–2 个 task；
7. 根据冷启动证据修订 SPEC/PLAN 并记录 diff；
8. 定义状态模型、动作 schema 和可注入 LLM 接口；
9. 以 mock LLM 和 TDD 实现最小主循环；
10. 依次用 TDD 实现工具、治理、反馈、记忆、配置和停机；
11. 深入实现选定的主贡献维度；
12. 完成三个离线、可重复的机制演示；
13. 接入真实 LLM 适配器和安全凭据管理；
14. 完成 WebUI、分发、双 CI 和公网部署；
15. 在干净环境做从零验收，并扫描 Git 历史中的 secret；
16. 学生本人完成反思，按最终清单逐项核验。

上述每个实现步骤还要继续拆为单个新鲜 subagent 可完成的小 task，并逐项执行红—绿—重构、spec 合规评审和代码质量评审。

## 20. 最容易失分的地方

- 把“使用 AI 编程工具”误当成“遵循 Superpowers 流程”；
- 在 SPEC、PLAN 和冷启动完成前编写正式实现；
- 先写实现，最后统一补测试和过程文档；
- 直接使用现成 agent loop 或宿主工具的内核能力；
- 只有 prompt 约束，没有确定性代码护栏；
- 让 LLM 自己判断测试结果，没有代码反馈解析器；
- 六维中漏掉记忆、配置或停机；
- 六个维度都很浅，没有明确 main contribution；
- mock 只是形式接口，测试仍访问网络或真实 LLM；
- mock 无视反馈、只播放固定序列，无法证明反馈改变决策；
- 只测试护栏函数，未证明危险动作没有到达执行层；
- 用单个演示脚本代替各核心机制的单元测试；
- 把规则文件、配置数量或提示词长度当作工程深度；
- 陌生 agent 与主 agent 类型相同、继承历史或得到口头解释；
- 只提供 `.env`，却声称完成了无风险的安全存储；
- 只有源码启动步骤，没有真正的分发产物；
- 所有代码一次 commit，无法核验 TDD、worktree、subagent 和评审过程；
- 忽略 GitHub Actions 与 `.gitlab-ci.yml` 的双重口径；
- 将云部署理解为可完全省略，未提交 WebUI URL；
- 让 AI 代写 `REFLECTION.md`，违反学术规范。

## 21. 提交前最终自检

- [ ] 能指出自研主循环的明确代码入口；
- [ ] 真实 LLM 可替换为 mock/stub；
- [ ] 无网络、无 API Key 时核心测试全部可运行；
- [ ] 决策、工具、记忆、治理、反馈、配置六维均有最低实现；
- [ ] 工具分发、治理、反馈、记忆和停机都有确定性测试；
- [ ] 危险动作由代码拦截，而非 prompt 提醒；
- [ ] 客观反馈由代码解析并真正进入下一轮上下文；
- [ ] 一个重点维度已做深，并与 SPEC、测试、演示一致；
- [ ] 三个指定机制演示均有稳定命令和预期结果；
- [ ] 未借用现成 agent runner 完成核心循环；
- [ ] `SPEC.md` 含“领域与机制设计”；
- [ ] `SPEC_PROCESS.md` 含 3 轮迭代、冷启动证据和关键 diff；
- [ ] 每个 task 均有红—绿—重构和两阶段评审证据；
- [ ] worktree、PR、subagent、人工修改和 commit 可追踪；
- [ ] 凭据录入、存储、状态、更新、清除和脱敏完整；
- [ ] 仓库当前内容及 Git 历史无真实凭据；
- [ ] 至少一种分发产物能在干净环境从零运行；
- [ ] README 规定章节完整；
- [ ] `.gitlab-ci.yml` 含 `unit-test`，并已处理 GitHub Actions 口径；
- [ ] 最后一次 CI/CD 为 pass；
- [ ] 公网 WebUI 截止前可访问；
- [ ] `REFLECTION.md` 为本人撰写，字数 1500–2500。

## 22. 原文未明确、需关注后续通知的事项

当前两份文档没有明确给出：

- 截止日期；
- 量化评分比例或 rubric；
- NJU Git 与 GitHub 是否最终都必须长期保留；
- 纯 CLI 项目的 WebUI 最低功能范围；
- CI/CD 执行记录的指定提交形式；
- 冷启动 agent 的试作代码是否必须保留在正式仓库。

在教师没有给出更新的书面说明前，建议采用本文第 14 节的双满足方案，并保存全部过程证据。

## 23. 最终判断标准

本项目是否真正合格，可以归结为一句话：

> 当真实 LLM 被移除、网络被切断、模型输出完全由测试控制时，你自行编写的 harness 是否仍能确定地完成动作分发、危险拦截、反馈回灌、记忆读写和正确停机，并清楚展示一个足够深入的主要机制贡献；同时，整个项目是否有完整可信的 Superpowers、TDD、评审、安全、分发、CI 和部署证据？

只有技术产品与工程过程同时满足，才算完整对齐“通用要求 + 自选任务一要求”。
